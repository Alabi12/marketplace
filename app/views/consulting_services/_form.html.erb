<%= form_with(model: consulting_service) do |form| %>
  <% if consulting_service.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(consulting_service.errors.count, "error") %> prohibited this consulting_service from being saved:</h2>

      <ul>
        <% consulting_service.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.textarea :description %>
  </div>

  <div>
    <%= form.label :business_consulting_profile_id, style: "display: block" %>
    <%= form.text_field :business_consulting_profile_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
